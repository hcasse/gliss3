# Generated by $(version) ($(date))
	
SOURCES = gliss.c$(foreach modules) \
	$(name).c$(end)

OBJECTS=$$(SOURCES:.c=.o)
CLEAN = $$(OBJECTS) lib$(proc).a
	
all: lib$(proc).a

lib$(proc).a: $$(OBJECTS)
	ar rcsu $$@ $$^
	ranlib $$@

clean:
	rm -rf $$(CLEAN)
