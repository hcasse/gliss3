# Generated by $(version) ($(date))

SOURCES = api.c \
	$(foreach modules) \
	$(name).c$(end)$(foreach sources) \
	$(path)$(end)

OBJECTS=$$(SOURCES:.c=.o)

CLEAN = $$(OBJECTS)
DISTCLEAN = lib$(proc).a

CFLAGS += -g3 $(foreach modules)$(CFLAGS)$(end) -O3 -I../include -Dassert

all: lib$(proc).a

lib$(proc).a: $$(OBJECTS)
	ar rcsu $$@ $$^
	ranlib $$@

clean:
	-rm -rf $$(CLEAN)

distclean: clean
	-rm -rf $$(DISTCLEAN)

# other dependencies
fetch.o: fetch_table.h
code.o: code_table.h
decode.o: decode_table.h
