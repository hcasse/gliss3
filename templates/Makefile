# Generated by $(version) ($(date))

SOURCES = api.c \
	$(foreach modules) \
	$(name).c$(end)$(foreach sources) \
	$(path)$(end)

OBJECTS=$$(SOURCES:.c=.o)

CLEAN = $$(OBJECTS)
DISTCLEAN = lib$(proc).a

CFLAGS += -g3 $(foreach modules)$(CFLAGS)$(end)

all: lib$(proc).a

lib$(proc).a: $$(OBJECTS)
	ar rcsu $$@ $$^
	ranlib $$@

clean:
	rm -rf $$(CLEAN)

distclean: clean
	rm -rf $$(DISTCLEAN)
