# Generated by $(version) ($(date))
	
SOURCES = gliss.c \
	api.c$(foreach modules) \
	$(name).c$(end)$(foreach sources) \
	$(path)$(end)

OBJECTS=$$(SOURCES:.c=.o)
CLEAN = $$(OBJECTS) lib$(proc).a
	
all: lib$(proc).a

lib$(proc).a: $$(OBJECTS)
	ar rcsu $$@ $$^
	ranlib $$@

clean:
	rm -rf $$(CLEAN)
